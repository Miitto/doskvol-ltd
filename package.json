{
  "scripts": {
    "build": "tailwindcss -i ./input.css -o ./assets/tailwind.css --minify",
    "watch": "tailwindcss -i ./input.css -o ./assets/tailwind.css --minify --watch"
  },
  "dependencies": {
    "tailwindcss": "^4.1.12",
    "@tailwindcss/cli": "^4.1.12"
  }
}
